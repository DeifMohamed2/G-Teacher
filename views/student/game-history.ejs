<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Game History | ELKABLY</title>
  
  <!-- Favicon -->
  <link rel="icon" type="image/png" href="/images/KImage.png">
  <link rel="shortcut icon" type="image/png" href="/images/KImage.png">
  <link rel="apple-touch-icon" href="/images/KImage.png">
  
  <link rel="stylesheet" href="/css/studentCSS/student-main.css">
  <link rel="stylesheet" href="/css/studentCSS/games.css">
  <link rel="stylesheet" href="/css/theme-toggle.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <meta name="description" content="View your past multiplayer quiz game results">
  <meta name="theme-color" content="#dc2626">
  
  <style>
    /* Game History Specific Styles */
    .game-history-page {
      background: var(--games-bg-secondary);
      min-height: 100vh;
    }

    .game-history-page .student-container {
      padding: var(--spacing-lg);
    }

    /* Statistics Cards */
    .stats-overview {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: var(--spacing-lg);
      margin-bottom: var(--spacing-xl);
    }

    .stat-card {
      background: linear-gradient(135deg, var(--games-bg-card), var(--games-bg-secondary));
      border: 1px solid var(--games-border);
      border-radius: var(--games-radius-xl);
      padding: var(--spacing-lg);
      box-shadow: var(--games-shadow-md);
      transition: var(--games-transition);
      position: relative;
      overflow: hidden;
    }

    .stat-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: linear-gradient(90deg, var(--games-primary), var(--games-info), var(--games-success));
    }

    .stat-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--games-shadow-lg);
    }

    .stat-card-icon {
      width: 48px;
      height: 48px;
      border-radius: var(--games-radius-lg);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      margin-bottom: var(--spacing-md);
      background: var(--games-primary-light);
      color: var(--games-primary);
    }

    .stat-card-value {
      font-size: 2rem;
      font-weight: 700;
      color: var(--games-text-primary);
      margin-bottom: var(--spacing-xs);
      line-height: 1;
    }

    .stat-card-label {
      font-size: 0.875rem;
      color: var(--games-text-secondary);
      text-transform: uppercase;
      letter-spacing: 0.05em;
      font-weight: 600;
    }

    /* Filters Section */
    .filters-section {
      background: var(--games-bg-card);
      border: 1px solid var(--games-border);
      border-radius: var(--games-radius-xl);
      padding: var(--spacing-lg);
      margin-bottom: var(--spacing-xl);
      box-shadow: var(--games-shadow-sm);
    }

    .filters-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: var(--spacing-lg);
      padding-bottom: var(--spacing-md);
      border-bottom: 2px solid var(--games-border);
    }

    .filters-title {
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--games-text-primary);
      display: flex;
      align-items: center;
      gap: var(--spacing-sm);
    }

    .filters-toggle {
      background: var(--games-primary);
      color: white;
      border: none;
      padding: var(--spacing-sm) var(--spacing-md);
      border-radius: var(--games-radius-md);
      cursor: pointer;
      font-weight: 600;
      transition: var(--games-transition);
      display: flex;
      align-items: center;
      gap: var(--spacing-xs);
    }

    .filters-toggle:hover {
      background: var(--games-primary-hover);
    }

    .filters-content {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: var(--spacing-md);
      transition: var(--games-transition);
    }

    .filters-content.collapsed {
      display: none;
    }

    .filter-group {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-xs);
    }

    .filter-label {
      font-size: 0.875rem;
      font-weight: 600;
      color: var(--games-text-primary);
      display: flex;
      align-items: center;
      gap: var(--spacing-xs);
    }

    .filter-input,
    .filter-select {
      padding: var(--spacing-sm) var(--spacing-md);
      border: 1px solid var(--games-border);
      border-radius: var(--games-radius-md);
      background: var(--games-bg-primary);
      color: var(--games-text-primary);
      font-size: 0.875rem;
      transition: var(--games-transition);
    }

    .filter-input:focus,
    .filter-select:focus {
      outline: none;
      border-color: var(--games-primary);
      box-shadow: 0 0 0 3px rgba(220, 38, 38, 0.1);
    }

    .filter-actions {
      display: flex;
      gap: var(--spacing-sm);
      margin-top: var(--spacing-md);
      grid-column: 1 / -1;
    }

    .btn-filter {
      padding: var(--spacing-sm) var(--spacing-lg);
      border: none;
      border-radius: var(--games-radius-md);
      font-weight: 600;
      cursor: pointer;
      transition: var(--games-transition);
      display: flex;
      align-items: center;
      gap: var(--spacing-xs);
    }

    .btn-filter-primary {
      background: var(--games-primary);
      color: white;
    }

    .btn-filter-primary:hover {
      background: var(--games-primary-hover);
    }

    .btn-filter-secondary {
      background: var(--games-bg-secondary);
      color: var(--games-text-primary);
      border: 1px solid var(--games-border);
    }

    .btn-filter-secondary:hover {
      background: var(--games-bg-tertiary);
    }

    /* Results Header */
    .results-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: var(--spacing-lg);
      flex-wrap: wrap;
      gap: var(--spacing-md);
    }

    .results-count {
      font-size: 1rem;
      color: var(--games-text-secondary);
      font-weight: 600;
    }

    .results-count strong {
      color: var(--games-primary);
      font-size: 1.25rem;
    }

    .sort-group {
      display: flex;
      align-items: center;
      gap: var(--spacing-sm);
    }

    .sort-label {
      font-size: 0.875rem;
      color: var(--games-text-secondary);
      font-weight: 600;
    }

    .sort-select {
      padding: var(--spacing-sm) var(--spacing-md);
      border: 1px solid var(--games-border);
      border-radius: var(--games-radius-md);
      background: var(--games-bg-card);
      color: var(--games-text-primary);
      font-size: 0.875rem;
      cursor: pointer;
      transition: var(--games-transition);
    }

    .sort-select:focus {
      outline: none;
      border-color: var(--games-primary);
      box-shadow: 0 0 0 3px rgba(220, 38, 38, 0.1);
    }

    /* Game History Grid */
    .game-history-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(380px, 1fr));
      gap: var(--spacing-xl);
      margin-bottom: var(--spacing-xl);
    }

    /* Enhanced Game History Card */
    .game-history-card {
      background: var(--games-bg-card);
      border: 1px solid var(--games-border);
      border-radius: var(--games-radius-xl);
      overflow: hidden;
      transition: var(--games-transition);
      box-shadow: var(--games-shadow-sm);
      position: relative;
      display: flex;
      flex-direction: column;
    }

    .game-history-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: linear-gradient(90deg, var(--games-primary), var(--games-info));
      opacity: 0;
      transition: var(--games-transition);
    }

    .game-history-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--games-shadow-lg);
      border-color: var(--games-primary);
    }

    .game-history-card:hover::before {
      opacity: 1;
    }

    .history-card-header {
      padding: var(--spacing-lg);
      background: linear-gradient(135deg, var(--games-bg-secondary), var(--games-bg-tertiary));
      border-bottom: 1px solid var(--games-border);
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: var(--spacing-md);
    }

    .game-info {
      flex: 1;
    }

    .game-title {
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--games-text-primary);
      margin: 0 0 var(--spacing-sm) 0;
      line-height: 1.3;
      background: linear-gradient(135deg, var(--games-text-primary), var(--games-primary));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .game-meta {
      display: flex;
      gap: var(--spacing-sm);
      flex-wrap: wrap;
    }

    .game-category {
      background: var(--games-primary-light);
      color: var(--games-primary);
      padding: 0.25rem 0.75rem;
      border-radius: var(--games-radius-md);
      font-size: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .game-difficulty {
      padding: 0.25rem 0.75rem;
      border-radius: var(--games-radius-md);
      font-size: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .difficulty-easy {
      background: rgba(16, 185, 129, 0.1);
      color: var(--games-success);
    }

    .difficulty-medium {
      background: rgba(245, 158, 11, 0.1);
      color: var(--games-warning);
    }

    .difficulty-hard {
      background: rgba(239, 68, 68, 0.1);
      color: var(--games-danger);
    }

    .game-date {
      font-size: 0.875rem;
      color: var(--games-text-secondary);
      font-weight: 600;
      white-space: nowrap;
      display: flex;
      align-items: center;
      gap: var(--spacing-xs);
    }

    .history-card-content {
      padding: var(--spacing-lg);
      flex: 1;
    }

    .performance-stats {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: var(--spacing-md);
      margin-bottom: var(--spacing-lg);
    }

    .stat-item {
      background: var(--games-bg-secondary);
      border: 1px solid var(--games-border);
      border-radius: var(--games-radius-lg);
      padding: var(--spacing-md);
      display: flex;
      align-items: center;
      gap: var(--spacing-md);
      transition: var(--games-transition);
    }

    .stat-item:hover {
      background: var(--games-bg-tertiary);
      transform: translateY(-2px);
    }

    .stat-icon {
      width: 40px;
      height: 40px;
      border-radius: var(--games-radius-md);
      background: var(--games-primary-light);
      color: var(--games-primary);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.125rem;
      flex-shrink: 0;
    }

    .stat-content {
      flex: 1;
      display: flex;
      flex-direction: column;
    }

    .stat-value {
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--games-text-primary);
      line-height: 1;
      margin-bottom: var(--spacing-xs);
    }

    .stat-label {
      font-size: 0.75rem;
      color: var(--games-text-secondary);
      text-transform: uppercase;
      letter-spacing: 0.05em;
      font-weight: 600;
    }

    .ranking-info {
      margin-bottom: var(--spacing-lg);
    }

    .ranking-badge {
      display: inline-flex;
      align-items: center;
      gap: var(--spacing-sm);
      padding: var(--spacing-sm) var(--spacing-lg);
      border-radius: var(--games-radius-lg);
      font-weight: 700;
      font-size: 0.875rem;
    }

    .ranking-badge.position-1 {
      background: linear-gradient(135deg, #ffd700, #ffed4e);
      color: #856404;
      box-shadow: 0 4px 12px rgba(255, 215, 0, 0.3);
    }

    .ranking-badge.position-2 {
      background: linear-gradient(135deg, #c0c0c0, #e6e6e6);
      color: #4a5568;
      box-shadow: 0 4px 12px rgba(192, 192, 192, 0.3);
    }

    .ranking-badge.position-3 {
      background: linear-gradient(135deg, #cd7f32, #e6b485);
      color: #744210;
      box-shadow: 0 4px 12px rgba(205, 127, 50, 0.3);
    }

    .ranking-badge.position-4,
    .ranking-badge.position-5,
    .ranking-badge.position-6,
    .ranking-badge.position-7,
    .ranking-badge.position-8 {
      background: var(--games-bg-secondary);
      color: var(--games-text-primary);
      border: 1px solid var(--games-border);
    }

    .session-details {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-sm);
      padding-top: var(--spacing-md);
      border-top: 1px solid var(--games-border);
    }

    .detail-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 0.875rem;
    }

    .detail-label {
      color: var(--games-text-secondary);
      font-weight: 600;
    }

    .detail-value {
      color: var(--games-text-primary);
      font-weight: 700;
    }

    .history-card-footer {
      padding: var(--spacing-md) var(--spacing-lg);
      background: var(--games-bg-secondary);
      border-top: 1px solid var(--games-border);
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: var(--spacing-sm);
    }

    .session-status {
      display: flex;
      align-items: center;
    }

    .status-badge {
      display: inline-flex;
      align-items: center;
      gap: var(--spacing-xs);
      padding: 0.375rem 0.75rem;
      border-radius: var(--games-radius-md);
      font-size: 0.75rem;
      font-weight: 600;
    }

    .status-badge.completed {
      background: rgba(16, 185, 129, 0.1);
      color: var(--games-success);
      border: 1px solid rgba(16, 185, 129, 0.2);
    }

    .session-time {
      font-size: 0.75rem;
      color: var(--games-text-muted);
      font-weight: 500;
    }

    /* Enhanced Empty State */
    .empty-state {
      text-align: center;
      padding: calc(var(--spacing-xl) * 3) var(--spacing-xl);
      background: linear-gradient(135deg, var(--games-bg-card), var(--games-bg-secondary));
      border: 2px dashed var(--games-border);
      border-radius: var(--games-radius-xl);
      box-shadow: var(--games-shadow-md);
      position: relative;
      overflow: hidden;
      animation: fadeInUp 0.6s ease-out;
    }

    .empty-state::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(220, 38, 38, 0.05) 0%, transparent 70%);
      animation: rotate 20s linear infinite;
    }

    .empty-state::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(135deg, 
        rgba(220, 38, 38, 0.03) 0%, 
        transparent 50%, 
        rgba(59, 130, 246, 0.03) 100%);
      pointer-events: none;
    }

    .empty-state-content {
      position: relative;
      z-index: 1;
    }

    .empty-state-icon-wrapper {
      position: relative;
      display: inline-block;
      margin-bottom: var(--spacing-xl);
    }

    .empty-state-icon {
      font-size: 8rem;
      background: linear-gradient(135deg, var(--games-primary), var(--games-info));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: var(--spacing-lg);
      animation: float 3s ease-in-out infinite;
      position: relative;
      display: inline-block;
    }

    .empty-state-icon::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 120%;
      height: 120%;
      background: radial-gradient(circle, rgba(220, 38, 38, 0.1) 0%, transparent 70%);
      border-radius: 50%;
      animation: pulse 2s ease-in-out infinite;
    }

    .empty-state-icon.filter-icon {
      background: linear-gradient(135deg, var(--games-warning), var(--games-primary));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .empty-state-icon.game-icon {
      background: linear-gradient(135deg, var(--games-primary), var(--games-success));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .empty-state h3 {
      color: var(--games-text-primary);
      font-size: 2rem;
      font-weight: 700;
      margin: 0 0 var(--spacing-md) 0;
      background: linear-gradient(135deg, var(--games-text-primary), var(--games-primary));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      position: relative;
    }

    .empty-state p {
      color: var(--games-text-secondary);
      font-size: 1.125rem;
      line-height: 1.8;
      margin: 0 0 var(--spacing-xl) 0;
      max-width: 500px;
      margin-left: auto;
      margin-right: auto;
    }

    .empty-state-actions {
      display: flex;
      gap: var(--spacing-md);
      justify-content: center;
      flex-wrap: wrap;
      margin-top: var(--spacing-xl);
    }

    .empty-state .btn {
      padding: var(--spacing-md) var(--spacing-xl);
      font-size: 1rem;
      font-weight: 600;
      border-radius: var(--games-radius-lg);
      transition: var(--games-transition);
      display: inline-flex;
      align-items: center;
      gap: var(--spacing-sm);
      box-shadow: var(--games-shadow-md);
      position: relative;
      overflow: hidden;
    }

    .empty-state .btn::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 0;
      height: 0;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.3);
      transform: translate(-50%, -50%);
      transition: width 0.6s, height 0.6s;
    }

    .empty-state .btn:hover::before {
      width: 300px;
      height: 300px;
    }

    .empty-state .btn-primary {
      background: linear-gradient(135deg, var(--games-primary), var(--games-primary-hover));
      color: white;
      border: none;
    }

    .empty-state .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: var(--games-shadow-lg);
    }

    .empty-state .btn-secondary {
      background: var(--games-bg-card);
      color: var(--games-text-primary);
      border: 2px solid var(--games-border);
    }

    .empty-state .btn-secondary:hover {
      background: var(--games-bg-secondary);
      border-color: var(--games-primary);
      color: var(--games-primary);
    }

    .empty-state-decoration {
      position: absolute;
      width: 100px;
      height: 100px;
      border-radius: 50%;
      background: linear-gradient(135deg, rgba(220, 38, 38, 0.1), rgba(59, 130, 246, 0.1));
      opacity: 0.3;
      animation: float 6s ease-in-out infinite;
    }

    .empty-state-decoration:nth-child(1) {
      top: 10%;
      left: 10%;
      animation-delay: 0s;
    }

    .empty-state-decoration:nth-child(2) {
      top: 20%;
      right: 15%;
      animation-delay: 1s;
      width: 60px;
      height: 60px;
    }

    .empty-state-decoration:nth-child(3) {
      bottom: 15%;
      left: 20%;
      animation-delay: 2s;
      width: 80px;
      height: 80px;
    }

    .empty-state-decoration:nth-child(4) {
      bottom: 10%;
      right: 10%;
      animation-delay: 1.5s;
      width: 50px;
      height: 50px;
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes float {
      0%, 100% {
        transform: translateY(0px);
      }
      50% {
        transform: translateY(-20px);
      }
    }

    @keyframes pulse {
      0%, 100% {
        opacity: 0.5;
        transform: translate(-50%, -50%) scale(1);
      }
      50% {
        opacity: 0.8;
        transform: translate(-50%, -50%) scale(1.1);
      }
    }

    @keyframes rotate {
      from {
        transform: rotate(0deg);
      }
      to {
        transform: rotate(360deg);
      }
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .game-history-page .student-container {
        padding: var(--spacing-md);
      }

      .stats-overview {
        grid-template-columns: repeat(2, 1fr);
        gap: var(--spacing-md);
      }

      .filters-content {
        grid-template-columns: 1fr;
      }

      .game-history-grid {
        grid-template-columns: 1fr;
        gap: var(--spacing-lg);
      }

      .performance-stats {
        grid-template-columns: 1fr;
      }

      .results-header {
        flex-direction: column;
        align-items: stretch;
      }
    }

    @media (max-width: 576px) {
      .stats-overview {
        grid-template-columns: 1fr;
      }

      .stat-card-value {
        font-size: 1.5rem;
      }

      .empty-state {
        padding: calc(var(--spacing-xl) * 2) var(--spacing-lg);
      }

      .empty-state-icon {
        font-size: 5rem !important;
      }

      .empty-state h3 {
        font-size: 1.5rem;
      }

      .empty-state p {
        font-size: 1rem;
      }

      .empty-state-actions {
        flex-direction: column;
        width: 100%;
      }

      .empty-state .btn {
        width: 100%;
        justify-content: center;
      }
    }
  </style>
</head>
<body class="<%= theme %>-theme">
  <div class="student-layout">
    <%- include('partials/student-sidebar', { currentPage: 'game-rooms', student: student }) %>
    <main class="student-main">
      <%- include('partials/student-header', { title: 'My Game History', student: student, theme: theme, currentPage: 'game-rooms' }) %>


        <!-- Statistics Overview -->
        <% if (sessions && sessions.length > 0) { %>
        <div class="stats-overview">
          <div class="stat-card">
            <div class="stat-card-icon">
              <i class="fas fa-trophy"></i>
            </div>
            <div class="stat-card-value"><%= statistics.totalSessions %></div>
            <div class="stat-card-label">Total Games</div>
          </div>
          <div class="stat-card">
            <div class="stat-card-icon">
              <i class="fas fa-star"></i>
            </div>
            <div class="stat-card-value"><%= statistics.avgScore %></div>
            <div class="stat-card-label">Average Score</div>
          </div>
          <div class="stat-card">
            <div class="stat-card-icon">
              <i class="fas fa-bullseye"></i>
            </div>
            <div class="stat-card-value"><%= statistics.overallAccuracy %>%</div>
            <div class="stat-card-label">Overall Accuracy</div>
          </div>
          <div class="stat-card">
            <div class="stat-card-icon">
              <i class="fas fa-crown"></i>
            </div>
            <div class="stat-card-value"><%= statistics.wins %></div>
            <div class="stat-card-label">Wins</div>
          </div>
          <div class="stat-card">
            <div class="stat-card-icon">
              <i class="fas fa-fire"></i>
            </div>
            <div class="stat-card-value"><%= statistics.bestScore %></div>
            <div class="stat-card-label">Best Score</div>
          </div>
        </div>
        <% } %>

        <!-- Filters Section -->
        <% if (sessions && sessions.length > 0) { %>
        <div class="filters-section">
          <div class="filters-header">
            <h2 class="filters-title">
              <i class="fas fa-filter"></i>
              Filters & Search
            </h2>
            <button class="filters-toggle" id="toggleFilters">
              <i class="fas fa-chevron-down" id="toggleIcon"></i>
              <span>Toggle Filters</span>
            </button>
          </div>
          <div class="filters-content" id="filtersContent">
            <div class="filter-group">
              <label class="filter-label">
                <i class="fas fa-search"></i>
                Search
              </label>
              <input type="text" id="searchInput" class="filter-input" placeholder="Search by game title or room code...">
            </div>
            <div class="filter-group">
              <label class="filter-label">
                <i class="fas fa-tags"></i>
                Category
              </label>
              <select id="categoryFilter" class="filter-select">
                <option value="">All Categories</option>
                <% categories.forEach(cat => { %>
                <option value="<%= cat %>"><%= cat %></option>
                <% }); %>
              </select>
            </div>
            <div class="filter-group">
              <label class="filter-label">
                <i class="fas fa-signal"></i>
                Difficulty
              </label>
              <select id="difficultyFilter" class="filter-select">
                <option value="">All Difficulties</option>
                <% difficulties.forEach(diff => { %>
                <option value="<%= diff %>"><%= diff.charAt(0).toUpperCase() + diff.slice(1) %></option>
                <% }); %>
              </select>
            </div>
            <div class="filter-group">
              <label class="filter-label">
                <i class="fas fa-calendar"></i>
                Date From
              </label>
              <input type="date" id="dateFrom" class="filter-input">
            </div>
            <div class="filter-group">
              <label class="filter-label">
                <i class="fas fa-calendar"></i>
                Date To
              </label>
              <input type="date" id="dateTo" class="filter-input">
            </div>
            <div class="filter-group">
              <label class="filter-label">
                <i class="fas fa-trophy"></i>
                Min Score
              </label>
              <input type="number" id="minScore" class="filter-input" placeholder="0" min="0">
            </div>
            <div class="filter-group">
              <label class="filter-label">
                <i class="fas fa-percentage"></i>
                Min Accuracy
              </label>
              <input type="number" id="minAccuracy" class="filter-input" placeholder="0" min="0" max="100">
            </div>
            <div class="filter-group">
              <label class="filter-label">
                <i class="fas fa-medal"></i>
                Position
              </label>
              <select id="positionFilter" class="filter-select">
                <option value="">All Positions</option>
                <option value="1">1st Place</option>
                <option value="2">2nd Place</option>
                <option value="3">3rd Place</option>
                <option value="top3">Top 3</option>
              </select>
            </div>
            <div class="filter-actions">
              <button class="btn-filter btn-filter-primary" id="applyFilters">
                <i class="fas fa-check"></i>
                Apply Filters
              </button>
              <button class="btn-filter btn-filter-secondary" id="clearFilters">
                <i class="fas fa-times"></i>
                Clear All
              </button>
            </div>
          </div>
        </div>
        <% } %>

        <!-- Results Header -->
        <% if (sessions && sessions.length > 0) { %>
        <div class="results-header">
          <div class="results-count">
            Showing <strong id="resultsCount"><%= sessions.length %></strong> of <strong><%= sessions.length %></strong> games
          </div>
          <div class="sort-group">
            <label class="sort-label">
              <i class="fas fa-sort"></i>
              Sort by:
            </label>
            <select id="sortSelect" class="sort-select">
              <option value="date-desc">Date (Newest First)</option>
              <option value="date-asc">Date (Oldest First)</option>
              <option value="score-desc">Score (Highest First)</option>
              <option value="score-asc">Score (Lowest First)</option>
              <option value="accuracy-desc">Accuracy (Highest First)</option>
              <option value="accuracy-asc">Accuracy (Lowest First)</option>
              <option value="time-desc">Time Spent (Longest First)</option>
              <option value="time-asc">Time Spent (Shortest First)</option>
            </select>
          </div>
        </div>
        <% } %>

        <!-- Game History Content -->
        <div class="game-history-container">
          <div class="game-history-grid" id="gameHistoryGrid">
            <% if (sessions && sessions.length > 0) { %>
              <% sessions.forEach(session => { %>
                <div class="game-history-card" 
                     data-category="<%= session.gameRoom?.category || '' %>"
                     data-difficulty="<%= session.gameRoom?.difficulty || '' %>"
                     data-date="<%= new Date(session.createdAt).toISOString().split('T')[0] %>"
                     data-score="<%= session.score || 0 %>"
                     data-accuracy="<%= session.accuracy || 0 %>"
                     data-position="<%= session.position || '' %>"
                     data-title="<%= (session.gameRoom?.title || '').toLowerCase() %>"
                     data-roomcode="<%= (session.gameRoom?.roomCode || '').toLowerCase() %>"
                     data-timespent="<%= session.timeSpent || 0 %>">
                  <div class="history-card-header">
                    <div class="game-info">
                      <h3 class="game-title"><%= session.gameRoom?.title || 'Unknown Game' %></h3>
                      <div class="game-meta">
                        <% if (session.gameRoom?.category) { %>
                        <span class="game-category"><%= session.gameRoom.category %></span>
                        <% } %>
                        <% if (session.gameRoom?.difficulty) { %>
                        <span class="game-difficulty difficulty-<%= session.gameRoom.difficulty %>">
                          <%= session.gameRoom.difficulty.charAt(0).toUpperCase() + session.gameRoom.difficulty.slice(1) %>
                        </span>
                        <% } %>
                      </div>
                    </div>
                    <div class="game-date">
                      <i class="fas fa-calendar-alt"></i>
                      <%= new Date(session.createdAt).toLocaleDateString() %>
                    </div>
                  </div>

                  <div class="history-card-content">
                    <div class="performance-stats">
                      <div class="stat-item">
                        <div class="stat-icon">
                          <i class="fas fa-trophy"></i>
                        </div>
                        <div class="stat-content">
                          <span class="stat-value"><%= session.score || 0 %></span>
                          <span class="stat-label">Final Score</span>
                        </div>
                      </div>
                      
                      <div class="stat-item">
                        <div class="stat-icon">
                          <i class="fas fa-check-circle"></i>
                        </div>
                        <div class="stat-content">
                          <span class="stat-value"><%= session.correctAnswers || 0 %>/<%= session.totalQuestions || 0 %></span>
                          <span class="stat-label">Correct Answers</span>
                        </div>
                      </div>
                      
                      <div class="stat-item">
                        <div class="stat-icon">
                          <i class="fas fa-percentage"></i>
                        </div>
                        <div class="stat-content">
                          <span class="stat-value"><%= session.accuracy || 0 %>%</span>
                          <span class="stat-label">Accuracy</span>
                        </div>
                      </div>
                      
                      <div class="stat-item">
                        <div class="stat-icon">
                          <i class="fas fa-clock"></i>
                        </div>
                        <div class="stat-content">
                          <span class="stat-value"><%= session.timeSpent || 0 %>s</span>
                          <span class="stat-label">Time Spent</span>
                        </div>
                      </div>
                    </div>

                    <% if (session.position) { %>
                      <div class="ranking-info">
                        <div class="ranking-badge position-<%= session.position %>">
                          <% if (session.position === 1) { %>
                            <i class="fas fa-crown"></i>
                            <span>1st Place</span>
                          <% } else if (session.position === 2) { %>
                            <i class="fas fa-medal"></i>
                            <span>2nd Place</span>
                          <% } else if (session.position === 3) { %>
                            <i class="fas fa-medal"></i>
                            <span>3rd Place</span>
                          <% } else { %>
                            <i class="fas fa-trophy"></i>
                            <span><%= session.position %>th Place</span>
                          <% } %>
                        </div>
                      </div>
                    <% } %>

                    <div class="session-details">
                      <% if (session.gameRoom?.roomCode) { %>
                      <div class="detail-item">
                        <span class="detail-label">Room Code:</span>
                        <span class="detail-value">#<%= session.gameRoom.roomCode %></span>
                      </div>
                      <% } %>
                      <% if (session.gameRoom?.questions) { %>
                      <div class="detail-item">
                        <span class="detail-label">Questions:</span>
                        <span class="detail-value"><%= Array.isArray(session.gameRoom.questions) ? session.gameRoom.questions.length : 0 %></span>
                      </div>
                      <% } %>
                      <% if (session.gameRoom?.totalTime) { %>
                      <div class="detail-item">
                        <span class="detail-label">Total Time:</span>
                        <span class="detail-value"><%= session.gameRoom.totalTime %> min</span>
                      </div>
                      <% } %>
                    </div>
                  </div>

                  <div class="history-card-footer">
                    <div class="session-status">
                      <span class="status-badge completed">
                        <i class="fas fa-check"></i>
                        Completed
                      </span>
                    </div>
                    <div class="session-time">
                      <i class="fas fa-clock"></i>
                      <%= new Date(session.createdAt).toLocaleString() %>
                    </div>
                  </div>
                </div>
              <% }); %>
            <% } else { %>
              <div class="empty-state" style="grid-column: 1 / -1;">
                <div class="empty-state-decoration"></div>
                <div class="empty-state-decoration"></div>
                <div class="empty-state-decoration"></div>
                <div class="empty-state-decoration"></div>
                <div class="empty-state-content">
                  <div class="empty-state-icon-wrapper">
                    <div class="empty-state-icon game-icon">
                      <i class="fas fa-gamepad"></i>
                    </div>
                  </div>
                  <h3>No Game History Yet</h3>
                  <p>You haven't completed any multiplayer quiz games yet. Join a game room, compete with others, and start building your gaming history!</p>
                  <div class="empty-state-actions">
                    <a href="/student/game-rooms" class="btn btn-primary">
                      <i class="fas fa-gamepad"></i>
                      Start Playing
                    </a>
                  </div>
                </div>
              </div>
            <% } %>
          </div>
        </div>
      </div>
    </main>
  </div>

  <script>
    // Filter and Sort Functionality
    (function() {
      const gameCards = Array.from(document.querySelectorAll('.game-history-card'));
      const searchInput = document.getElementById('searchInput');
      const categoryFilter = document.getElementById('categoryFilter');
      const difficultyFilter = document.getElementById('difficultyFilter');
      const dateFrom = document.getElementById('dateFrom');
      const dateTo = document.getElementById('dateTo');
      const minScore = document.getElementById('minScore');
      const minAccuracy = document.getElementById('minAccuracy');
      const positionFilter = document.getElementById('positionFilter');
      const sortSelect = document.getElementById('sortSelect');
      const applyFiltersBtn = document.getElementById('applyFilters');
      const clearFiltersBtn = document.getElementById('clearFilters');
      const resultsCount = document.getElementById('resultsCount');
      const toggleFiltersBtn = document.getElementById('toggleFilters');
      const filtersContent = document.getElementById('filtersContent');
      const toggleIcon = document.getElementById('toggleIcon');

      let allCards = [...gameCards];

      // Toggle filters visibility
      if (toggleFiltersBtn && filtersContent) {
        toggleFiltersBtn.addEventListener('click', () => {
          filtersContent.classList.toggle('collapsed');
          const isCollapsed = filtersContent.classList.contains('collapsed');
          toggleIcon.className = isCollapsed ? 'fas fa-chevron-down' : 'fas fa-chevron-up';
        });
      }

      function filterAndSort() {
        const searchTerm = (searchInput?.value || '').toLowerCase();
        const category = categoryFilter?.value || '';
        const difficulty = difficultyFilter?.value || '';
        const dateFromValue = dateFrom?.value || '';
        const dateToValue = dateTo?.value || '';
        const minScoreValue = parseInt(minScore?.value) || 0;
        const minAccuracyValue = parseInt(minAccuracy?.value) || 0;
        const positionValue = positionFilter?.value || '';

        let filtered = allCards.filter(card => {
          const cardCategory = card.dataset.category || '';
          const cardDifficulty = card.dataset.difficulty || '';
          const cardDate = card.dataset.date || '';
          const cardScore = parseInt(card.dataset.score) || 0;
          const cardAccuracy = parseInt(card.dataset.accuracy) || 0;
          const cardPosition = card.dataset.position || '';
          const cardTitle = card.dataset.title || '';
          const cardRoomCode = card.dataset.roomcode || '';

          // Search filter
          if (searchTerm && !cardTitle.includes(searchTerm) && !cardRoomCode.includes(searchTerm)) {
            return false;
          }

          // Category filter
          if (category && cardCategory !== category) {
            return false;
          }

          // Difficulty filter
          if (difficulty && cardDifficulty.toLowerCase() !== difficulty.toLowerCase()) {
            return false;
          }

          // Date filters
          if (dateFromValue && cardDate < dateFromValue) {
            return false;
          }
          if (dateToValue && cardDate > dateToValue) {
            return false;
          }

          // Score filter
          if (cardScore < minScoreValue) {
            return false;
          }

          // Accuracy filter
          if (cardAccuracy < minAccuracyValue) {
            return false;
          }

          // Position filter
          if (positionValue) {
            if (positionValue === 'top3') {
              if (!cardPosition || parseInt(cardPosition) > 3) {
                return false;
              }
            } else {
              if (cardPosition !== positionValue) {
                return false;
              }
            }
          }

          return true;
        });

        // Sort
        const sortValue = sortSelect?.value || 'date-desc';
        filtered.sort((a, b) => {
          switch(sortValue) {
            case 'date-desc':
              return new Date(b.dataset.date) - new Date(a.dataset.date);
            case 'date-asc':
              return new Date(a.dataset.date) - new Date(b.dataset.date);
            case 'score-desc':
              return parseInt(b.dataset.score) - parseInt(a.dataset.score);
            case 'score-asc':
              return parseInt(a.dataset.score) - parseInt(b.dataset.score);
            case 'accuracy-desc':
              return parseInt(b.dataset.accuracy) - parseInt(a.dataset.accuracy);
            case 'accuracy-asc':
              return parseInt(a.dataset.accuracy) - parseInt(b.dataset.accuracy);
            case 'time-desc':
              return parseInt(b.dataset.timespent) - parseInt(a.dataset.timespent);
            case 'time-asc':
              return parseInt(a.dataset.timespent) - parseInt(b.dataset.timespent);
            default:
              return 0;
          }
        });

        // Update DOM
        const grid = document.getElementById('gameHistoryGrid');
        if (grid) {
          grid.innerHTML = '';
          filtered.forEach(card => {
            grid.appendChild(card);
            card.style.animation = 'fadeIn 0.3s ease-in';
          });
        }

        // Update results count
        if (resultsCount) {
          resultsCount.textContent = filtered.length;
        }

        // Show empty state if no results
        if (filtered.length === 0 && grid) {
          grid.innerHTML = `
            <div class="empty-state" style="grid-column: 1 / -1;">
              <div class="empty-state-decoration"></div>
              <div class="empty-state-decoration"></div>
              <div class="empty-state-decoration"></div>
              <div class="empty-state-decoration"></div>
              <div class="empty-state-content">
                <div class="empty-state-icon-wrapper">
                  <div class="empty-state-icon filter-icon">
                    <i class="fas fa-filter"></i>
                  </div>
                </div>
                <h3>No Games Found</h3>
                <p>No games match your current filters. Try adjusting your search criteria or clear the filters to see all your games.</p>
                <div class="empty-state-actions">
                  <button class="btn btn-primary" onclick="clearAllFilters()">
                    <i class="fas fa-times"></i>
                    Clear Filters
                  </button>
                  <a href="/student/game-rooms" class="btn btn-secondary">
                    <i class="fas fa-gamepad"></i>
                    Play New Game
                  </a>
                </div>
              </div>
            </div>
          `;
        }
      }

      function clearAllFilters() {
        if (searchInput) searchInput.value = '';
        if (categoryFilter) categoryFilter.value = '';
        if (difficultyFilter) difficultyFilter.value = '';
        if (dateFrom) dateFrom.value = '';
        if (dateTo) dateTo.value = '';
        if (minScore) minScore.value = '';
        if (minAccuracy) minAccuracy.value = '';
        if (positionFilter) positionFilter.value = '';
        if (sortSelect) sortSelect.value = 'date-desc';
        filterAndSort();
      }

      // Event listeners
      if (applyFiltersBtn) {
        applyFiltersBtn.addEventListener('click', filterAndSort);
      }

      if (clearFiltersBtn) {
        clearFiltersBtn.addEventListener('click', clearAllFilters);
      }

      if (searchInput) {
        searchInput.addEventListener('input', filterAndSort);
      }

      if (sortSelect) {
        sortSelect.addEventListener('change', filterAndSort);
      }

      // Make clearAllFilters available globally for empty state button
      window.clearAllFilters = clearAllFilters;

      // Initialize with current filters
      filterAndSort();
    })();
  </script>
</body>
</html>
